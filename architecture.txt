━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                         输 入
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  graph_obs                           cand_obs
  ┌─────────────────────┐             ┌─────────────────────────┐
  │ node_types (N,) int │             │ face_types    (C,) int  │
  │ edge_src   (E,) int │             │ new_tet_types (C,) int  │
  │ edge_dst   (E,) int │             └─────────────────────────┘
  │ edge_types (E,) int │               N=四面体数, C=候选数
  └─────────────────────┘
    N=四面体数, E=边数

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      GraphEncoder
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  node_types (N,)
      │
      ▼
  ┌─────────────┐
  │  node_emb   │  Embedding(5, 128)        5种四面体类型 → 向量
  └─────────────┘
      │
      ▼  x: (N, 128)
      │
      │   edge_types (E,) ──→ +1偏移 ──→ 填入 edge_idx_mat (N,N)
      │                                   0=无边, 1-4=面类型+1
      │
  ┌───┴──────────────────────────────────────────────────────┐
  │               GraphTransformerLayer  × 3层               │
  │                                                          │
  │   x(N,128)  ──→  W_q/W_k/W_v  ──→  Q,K,V (N,H,32)      │
  │                                          │               │
  │   edge_idx_mat(N,N) ──→ edge_bias ──→ bias(H,N,N)        │
  │                           Embedding        │             │
  │                           (5, H=4)         ▼             │
  │                               scores = Q·Kᵀ/√32 + bias  │
  │                               (H,N,N)      │             │
  │                                            ▼             │
  │                                       softmax → attn     │
  │                                            │             │
  │                               out = attn · V → W_o       │
  │                                            │             │
  │                    x ← LayerNorm( x + out )              │
  │                    x ← LayerNorm( x + FFN(x) )           │
  └───────────────────────────────┬──────────────────────────┘
                                  │
                                  ▼  x: (N, 128)
                          ┌───────────────┐
                          │  mean(x, 0)   │  (128,)
                          │  max(x,  0)   │  (128,)
                          │   cat → (256,)│
                          └───────┬───────┘
                                  │
                              Linear(256→128)
                                  │
                                  ▼
                            global_emb (128,)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      CandidateScorer
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  global_emb (128,)       face_types (C,)       new_tet_types (C,)
       │                       │                       │
       │                       ▼                       ▼
       │               ┌─────────────┐         ┌─────────────┐
       │               │  face_emb   │         │ new_tet_emb │
       │               │ Emb(5,128)  │         │  Emb(5,128) │
       │               └─────────────┘         └─────────────┘
       │                   (C, 128)                 (C, 128)
       │                       │                       │
       ▼                       ▼                       ▼
  expand(C,128) ──────── cat([g, f, nt]) ────────────────────▶
                              (C, 384)
                                 │
                          Linear(384→128)
                          GELU
                          Linear(128→1)
                                 │
                                 ▼
                           scores (C,)
                                 │
                            log_softmax
                                 │
                           log_probs (C,)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                           输 出
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  采样/argmax → action_idx → 第 action_idx 个候选 → 放置新粒子
